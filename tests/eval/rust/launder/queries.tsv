# Query -> the unit that answers it, against a launder checkout — part of the
# first Rust SEARCH ground truth; see tests/eval/rust/trekr/queries.tsv for
# the band conventions.
#
# DRAFTED BY CLAUDE, NOT YET REVIEWED BY THE OWNER. Behaviour verified by
# reading the bodies.

# --- easy on purpose ---------------------------------------------------------
find email addresses in a log line	detect::net::email

# --- no token shared with the answer's identifier ----------------------------
give the same person the same masked name every time	registry::Registry::placeholder

# --- behaviour that appears in NO identifier ---------------------------------
# The keep_private branch lives in the body; the name is two letters.
mask public network addresses but leave private ones readable	detect::net::ip
