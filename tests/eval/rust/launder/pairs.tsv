# token_hash pairs against a launder checkout (run from ~/code/lib/rust/launder).
#
# DRAFTED BY CLAUDE, NOT YET REVIEWED BY THE OWNER. Verified by reading both
# bodies. See tests/eval/rust/trekr/pairs.tsv and the README for the tier's
# contract.

# Two `Self::default()` constructors on unrelated types: pure boilerplate,
# never a consolidation (DEC-020). They collide below --min-lines 4 (3 lines),
# so the default floor is exactly what stands between this pair and a false
# positive — the Rust datum for where the floor belongs.
Registry::new	Report::new	distinct
# Same detector scaffold; the regex static and the Kind/PType tokens differ,
# which is the entire behaviour.
email	mac	distinct
