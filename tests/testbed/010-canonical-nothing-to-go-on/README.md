Every signal is silent: one commit, so the two bodies are the same age; trekr
is unreachable in the testbed, so references is unavailable; and both owners
sit at the same namespace depth, so even the tiebreak has nothing.

The expected answer is no answer. A tool that produced a pick here would be
producing it out of nothing, and this case exists to make that fail.
