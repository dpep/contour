A rename is not a change, so these two are one group — and the one that was
committed first is the one to keep. The whole point of the case is that the
answer lives outside the bodies, which are identical after normalization.

`history` is what makes this testable: without it both files land in one
commit and every body is the same age.
